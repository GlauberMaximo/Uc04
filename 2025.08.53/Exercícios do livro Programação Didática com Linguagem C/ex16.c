//5.c)
//Objetivo: identificar os problamas no código
#include<stdio.h>
 int main() {
    int a1 = 5, b2 = -2; //anteriormente a variável 'a1' estava com o nome '1a'. O nome de variáveis não pode começar com números.

    printf("Valores a1 e b2: %d %d\n", a1,b2);

    return 0;
 }